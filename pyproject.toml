[project]
name = "llm-agentic-legal-retrieval"
version = "0.1.0"
requires-python = ">=3.9"
dependencies = [
    "pandas",
    "numpy",
    "scikit-learn",
    "tqdm",
    "jupyter",
    "ipykernel",
    "kaggle>=1.7.4.5",
]
